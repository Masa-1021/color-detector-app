# Create this secret before deploying:
#   kubectl -n circle-detector create secret generic oracle-credentials \
#     --from-literal=dsn='host:port/service' \
#     --from-literal=user='USERNAME' \
#     --from-literal=password='PASSWORD'
#
# This file is a template - do NOT commit real credentials.
apiVersion: v1
kind: Secret
metadata:
  name: oracle-credentials
  namespace: circle-detector
type: Opaque
stringData:
  dsn: "CHANGE_ME"
  user: "CHANGE_ME"
  password: "CHANGE_ME"
